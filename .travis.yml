language: node_js
node_js:
  - 4.2
sudo: false
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - build-essential
    - g++-4.8
env:
  matrix:
    - CXX=g++-4.8
before_script:
- npm install -g grunt-cli
